
mixin author(authorName)
  - var author = contents.authors[authorName + '.json'];
  span.author
    if author
      a(href='http://twitter.com/' + author.metadata.twitter)= author.metadata.name
    else
      = authorName
